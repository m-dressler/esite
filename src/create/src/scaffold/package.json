{
  "name": "{{PROJECT_NAME}}",
  "version": "0.0.1",
  "description": "Your newly created esite project",
  "scripts": {
    "deploy": "esite deploy",
    "preview": "esite exec preview",
    "dev": "esite exec preview"
  },
  "dependencies": {
    "@esite/core": "{{VERSION}}",
    "@esite/preview": "{{VERSION}}",
    "@esite/sass": "{{VERSION}}",
    "@esite/typescript": "{{VERSION}}",
    "@esite/minify": "{{VERSION}}",
    "@esite/cache-bust": "{{VERSION}}",
    "@esite/deploy-s3": "{{VERSION}}"
  }
}
