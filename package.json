{
  "name": "esite-repo-manager",
  "version": "0.6.1",
  "description": "",
  "main": "build.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ts-node --esm build.ts build",
    "publish": "ts-node --esm build.ts publish",
    "version": "ts-node --esm build.ts version",
    "debug": "ts-node --esm test/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "terser": "^5.24.0",
    "typescript": "^5.2.2"
  },
  "files": [
    "lib/**/*"
  ],
  "devDependencies": {
    "@types/node": "^20.8.9"
  }
}